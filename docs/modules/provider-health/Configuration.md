# Provider Health, Reliability & Quota-Aware Routing Scoring - Configuration

Configured via `ProviderHealthScoringPolicy` (`reliabilityWeight`, `latencyWeight`, `rateLimitWeight`, `quotaWeight`, `cooldownWeight`, `targetLatencyMs`, `degradedThreshold`, `unhealthyThreshold`).

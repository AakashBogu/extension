# Provider Routing Optimization & Adaptive Routing Policy - Configuration

Configured via `ProviderAdaptiveRoutingPolicy` (`emaAlpha`, `explorationBonusMax`, `minimumScoreDelta`, `stickinessBonus`, `requestType`, `weightsConfig`).

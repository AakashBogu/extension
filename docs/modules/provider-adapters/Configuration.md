# Concrete Provider Adapter Layer - Configuration

Configured via `OpenAIConfig`, `GeminiConfig`, `BraveConfig`, `BingConfig`, and resolved securely via `ProviderCredentialManager` and `SecretsManager`.

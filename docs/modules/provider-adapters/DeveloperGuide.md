# Concrete Provider Adapter Layer - Developer Guide

```typescript
import { ProviderBootstrap } from '@core/providers/bootstrap/ProviderBootstrap';

await ProviderBootstrap.bootstrap(config, aiRegistry, searchRegistry, credentialManager);
```

# Concrete Provider Adapter Layer - Responsibilities

Translates vendor-specific HTTP APIs into generic AIResponse / SearchResponse data models while isolating credentials and normalizing error responses.

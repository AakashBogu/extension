# Concrete Provider Adapter Layer - Event Specifications

Reuses Module 6B provider events (`provider.registered`, `provider.request_failed`, `provider.fallback_selected`). Zero secrets or API keys are ever emitted in event payloads.

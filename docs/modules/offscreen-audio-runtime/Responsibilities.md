# Offscreen Audio Runtime - Responsibilities

Establishes the isolated browser offscreen document runtime, AudioContext lifecycle abstraction, and type-safe messaging protocol without implementing tab audio capture, VAD, or STT.

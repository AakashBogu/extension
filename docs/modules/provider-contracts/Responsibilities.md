# AI & Search Provider Abstraction Layer — Provider Contracts - Responsibilities

Defines clean, vendor-agnostic type abstractions for external AI and search providers without hardcoding vendor dependencies or exposing credentials.

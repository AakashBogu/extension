# Real-Time Audio Processing & Voice Activity Detection Engine - Responsibilities

Processes raw audio buffers in memory to extract PCM, normalize sample rates to 16kHz, detect voice activity, and form speech segments without persisting audio or streaming raw PCM through global state.

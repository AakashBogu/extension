# Real-Time Audio Processing & Voice Activity Detection Engine - Configuration

Configured via `AudioProcessingConfig` (`targetSampleRate`, `channelMode`, `frameDurationMs`, `chunkDurationMs`, `maxPendingFrames`, `vad`).

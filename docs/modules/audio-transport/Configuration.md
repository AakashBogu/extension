# Real-Time Audio Transport, Bounded Chunk Queue & Speech Pipeline Boundary - Configuration

Configured via `AudioTransportConfig` (`maxQueueSize`, `dropStrategy`, `backpressureThresholdPercent`, `retry`).

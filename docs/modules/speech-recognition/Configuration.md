# Provider-Agnostic Speech Recognition Pipeline - Configuration

Configured via `SpeechRecognitionConfig` (`enabled`, `language`, `enablePartialResults`, `confidenceThreshold`, `providerPreference`, `fallbackEnabled`).

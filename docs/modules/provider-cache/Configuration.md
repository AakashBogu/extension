# Provider Response Caching & In-Flight Request Deduplication Layer - Configuration

Configured via `ProviderCachePolicy` (`aiTtlMs`, `searchTtlMs`, `maxEntries`, `maxApproximateSizeBytes`, `enabled`).

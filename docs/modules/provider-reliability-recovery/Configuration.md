# Provider Reliability / Recovery & Circuit-Breaker Integration - Configuration

Configured via `ProviderCircuitPolicy` (`failureThreshold`, `rollingFailureThreshold`, `minimumRollingSamples`, `openDurationMs`, `halfOpenProbeTimeoutMs`, `maxHalfOpenProbes`, `successesToClose`, `failureBackoffFactor`, `maxOpenDurationMs`, `enabled`).
